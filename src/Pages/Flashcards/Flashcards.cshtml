@page "/Flashcards/{categoryId?}"
@using ContosoCrafts.WebSite.Components
@model ContosoCrafts.WebSite.Pages.Flashcards.FlashcardsModel
@{
    ViewData["Title"] = "Flashcards";
}

@(await Html.RenderComponentAsync<FlashcardList>(RenderMode.ServerPrerendered))

