@page
@using ContosoCrafts.WebSite.Components
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<!-- Render the CategoryList component -->
@(await Html.RenderComponentAsync<CategoryList>(RenderMode.ServerPrerendered))
